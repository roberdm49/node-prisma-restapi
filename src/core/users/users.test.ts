import { cleanUpAll } from '@/utils/testing'

describe('Users', () => {
  beforeEach(async () => {
    await cleanUpAll()
  })

  describe('Happy paths', () => {

  })

  describe('Exception paths', () => {

  })
})
